import { createServerFn } from "@tanstack/react-start";
import { z } from "zod";
import { requireSupabaseAuth } from "@/integrations/supabase/auth-middleware";
function callerEmail(claims: unknown): string|null { const c=claims as {email?:string;is_anonymous?:boolean}|null; if(!c||c.is_anonymous)return null; return c.email??null; }
export const sendWelcomeLifecycleEmail=createServerFn({method:"POST"}).middleware([requireSupabaseAuth]).handler(async({context})=>{const email=callerEmail(context.claims);if(!email)return{sent:false,reason:"no-email"as const};const svc=await import("@/lib/email/service.server");if(await svc.alreadySent("welcome",email))return{sent:false,reason:"already-sent"as const};const name=(context.claims as {user_metadata?:{full_name?:string}})?.user_metadata?.full_name;const res=await svc.sendWelcomeEmail(email,name);return{sent:res.ok,reason:res.ok?("ok"as const):("failed"as const)};});
export const notifyReportStarted=createServerFn({method:"POST"}).inputValidator(d=>z.object({reportTitle:z.string().min(1).max(200)}).parse(d)).middleware([requireSupabaseAuth]).handler(async({data,context})=>{const email=callerEmail(context.claims);if(!email)return{sent:false};const{sendReportStartedEmail}=await import("@/lib/email/service.server");const res=await sendReportStartedEmail({to:email,reportTitle:data.reportTitle});return{sent:res.ok};});
export const notifyReportReady=createServerFn({method:"POST"}).inputValidator(d=>z.object({reportTitle:z.string().min(1).max(200)}).parse(d)).middleware([requireSupabaseAuth]).handler(async({data,context})=>{const email=callerEmail(context.claims);if(!email)return{sent:false};const db=context.supabase as any;const{data:delivery,error}=await db.from("report_deliveries").select("report_id,report_title").eq("user_id",context.userId).eq("report_title",data.reportTitle).maybeSingle();if(error)throw new Error(error.message);if(!delivery)return{sent:false,reason:"report-not-persisted"as const};const{createReportDownloadToken}=await import("@/lib/payments/download-token.server");const{sendReportReadyEmail}=await import("@/lib/email/service.server");const base=process.env.SITE_URL??process.env.PUBLIC_APP_URL??"https://cosmic-blueprint.space";const res=await sendReportReadyEmail({to:email,reportTitle:delivery.report_title,downloadUrl:`${base}/api/reports/download?token=${encodeURIComponent(createReportDownloadToken(context.userId,delivery.report_id))}`});return{sent:res.ok};});
export const sendSupportReply=createServerFn({method:"POST"}).inputValidator(d=>z.object({to:z.string().email(),name:z.string().max(200).optional(),ticketId:z.string().min(1).max(100),subject:z.string().min(1).max(200),message:z.string().min(1).max(5000)}).parse(d)).middleware([requireSupabaseAuth]).handler(async({data,context})=>{const{data:isAdmin}=await context.supabase.rpc("has_role",{_user_id:context.userId,_role:"admin"});if(!isAdmin)throw new Error("Admin access required");const paragraphs=data.message.split(/\n{2,}/).map(p=>`<p>${p.replace(/&/g,"&amp;").replace(/</g,"&lt;").replace(/>/g,"&gt;").replace(/\n/g,"<br/>")}</p>`).join("");const{sendSupportReplyEmail}=await import("@/lib/email/service.server");const res=await sendSupportReplyEmail({to:data.to,name:data.name,ticketId:data.ticketId,subject:data.subject,messageHtml:paragraphs});if(!res.ok)throw new Error(res.error);return{sent:true,logId:res.logId};});
